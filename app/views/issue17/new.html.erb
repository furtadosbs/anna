<h1><%= t '.title' %></h1>

<% if @user.issue17 %>
  <p><%= t('.already_answered') %></p>
<% else %>
  <%= form_with url: issue17_new_path, method: :post do |form| %>
    <div>
      <%= form.radio_button :answer, "nao", checked: true %>
      <%= form.label :answer, t('.option1'), value: "nao" %>
    </div>

    <div>
      <%= form.radio_button :answer, User::ISSUE17_ANSWER %>
      <%= form.label :answer, t('.option2'), value: User::ISSUE17_ANSWER %>
    </div>

    <div>
      <%= form.radio_button :answer, "so_doente" %>
      <%= form.label :answer, t('.option3'), value: "so_doente" %>
    </div>

    <div>
      <%= form.submit t('.submit') %>
    </div>
  <% end %>
<% end %>

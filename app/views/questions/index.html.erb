<% @stages.each do |stage| %>
  <section class="stage">
    <header class="stage__header">
      <h3>
        <%= stage.title %>
      </h3>
    </header>

    <ul class="stage__list">
      <% stage.questions.each do |question| %>
        <%= link_to question.description, new_question_answer_path(question), class: "stage__item" %>
      <% end %>
    </ul>
  </section>
<% end %>
